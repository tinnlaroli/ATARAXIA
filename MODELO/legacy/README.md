# Legacy Yelp / hybrid recommender

Código anterior (CF Pearson, LightFM, RF de estrellas Yelp) conservado en `../src/` pero **no importado** por `api.py`.

El servicio activo es el pipeline **wellness** (`stress_classifier`, `wellness_matchmaker`, `wellness_catalog`).
